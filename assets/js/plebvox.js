// assets/js/plebvox.js - PlebVox 3.1
// Recovered v2.9 mapping + word highlighting; retains current accessibility controls.
(function () {
    'use strict';
    let isReading=false,isPaused=false,utterance=null,availableVoices=[],selectedVoice=null;
    let speechRate=0.7,currentSectionData=null,activeControls=null,sectionDataList=[];
    let voicesLoaded=false,voiceLoadComplete=false,voicePromise=null,sectionCounter=0;
    let customHighlightSupported=false;
    const HIGHLIGHT_NAME='plebvox-current-word';
    function installHighlightStyle(){if(!document.getElementById('plebvox-highlight-style')){const s=document.createElement('style');s.id='plebvox-highlight-style';s.textContent='::highlight('+HIGHLIGHT_NAME+'){background:#ffeb3b;color:#000;text-shadow:none}.plebvox-highlight{background:#ffeb3b!important;color:#000!important;padding:0 2px;border-radius:2px;box-shadow:0 0 0 2px #f57c00}';document.head.appendChild(s);}customHighlightSupported=!!(window.CSS&&CSS.highlights&&typeof Highlight==='function');console.log('PlebVox 3.1: CSS Highlight API '+(customHighlightSupported?'available':'unavailable; fallback enabled'));}
    function clearHighlights(){if(customHighlightSupported)try{CSS.highlights.delete(HIGHLIGHT_NAME);}catch(e){}document.querySelectorAll('.plebvox-highlight').forEach(function(el){const p=el.parentNode;if(p){p.replaceChild(document.createTextNode(el.textContent||''),el);p.normalize();}});}
    function cleanSpeechText(text){let r=(text||'').replace(/\s+/g,' ');try{r=r.replace(/[\p{Extended_Pictographic}\p{Emoji_Presentation}\uFE0F\u200D]/gu,'');}catch(e){}return r;}
    function needsSeparator(t){return !!t&&!/[\s(\[{\"'“‘]$/.test(t);}
    function buildTextMapping(contentNodes){let speechText='',entries=[];const ignore=['SCRIPT','STYLE','NOSCRIPT','IMG','FIGURE','FIGCAPTION','SVG','CANVAS','VIDEO','AUDIO','IFRAME','OBJECT','EMBED','SOURCE','TRACK'];const blocks=['P','DIV','H1','H2','H3','H4','H5','H6','LI','BLOCKQUOTE','SECTION','ARTICLE','HEADER','FOOTER','MAIN','ASIDE'];function process(node){if(node.nodeType===Node.COMMENT_NODE)return;if(node.nodeType===Node.TEXT_NODE){const raw=node.textContent||'';if(!raw.trim())return;const normalized=cleanSpeechText(raw).trim();if(!normalized)return;if(speechText&&needsSeparator(speechText))speechText+=' ';speechText+=normalized;entries.push({node:node,rawText:raw,normalizedText:normalized});return;}if(node.nodeType!==Node.ELEMENT_NODE)return;const tag=node.tagName.toUpperCase();if(ignore.includes(tag))return;if(blocks.includes(tag)&&speechText&&needsSeparator(speechText))speechText+=' ';node.childNodes.forEach(process);if(blocks.includes(tag)&&speechText&&needsSeparator(speechText))speechText+=' ';}contentNodes.forEach(process);speechText=speechText.replace(/\s+/g,' ').trim();if(!speechText)return{speechText:'',mapping:[]};const mapping=[];let searchStart=0;entries.forEach(function(e){const i=speechText.indexOf(e.normalizedText,searchStart);if(i>=0){mapping.push({node:e.node,rawText:e.rawText,normalizedText:e.normalizedText,speechStart:i,speechEnd:i+e.normalizedText.length});searchStart=i+e.normalizedText.length;}});return{speechText:speechText,mapping:mapping};}
    function rebuildSectionMapping(section){if(!section||!section.startNode||!section.endNode)return;const nodes=[];let n=section.startNode.nextSibling;while(n&&n!==section.endNode){if(n.nodeType!==Node.COMMENT_NODE)nodes.push(n);n=n.nextSibling;}section.contentNodes=nodes;section.mappingData=buildTextMapping(nodes);section.text=section.mappingData.speechText;}
    function getPlebVoxSections(){const main=document.querySelector('main');if(!main)return[];const walker=document.createTreeWalker(main,NodeFilter.SHOW_COMMENT,{acceptNode:function(n){const t=(n.textContent||'').trim();return t==='PLEBVOX:START'||t==='PLEBVOX:END'?NodeFilter.FILTER_ACCEPT:NodeFilter.FILTER_REJECT;}});const markers=[];let n=walker.nextNode();while(n){markers.push({node:n,type:n.textContent.trim()});n=walker.nextNode();}const sections=[];let startNode=null;markers.forEach(function(m){if(m.type==='PLEBVOX:START'){if(!startNode)startNode=m.node;else console.warn('PlebVox: nested START marker');return;}if(m.type==='PLEBVOX:END'&&startNode){const endNode=m.node,nodes=[];let x=startNode.nextSibling;while(x&&x!==endNode){if(x.nodeType!==Node.COMMENT_NODE)nodes.push(x);x=x.nextSibling;}const md=buildTextMapping(nodes);if(md.speechText)sections.push({number:sections.length+1,text:md.speechText,startNode:startNode,endNode:endNode,contentNodes:nodes,mappingData:md,controlElement:null});startNode=null;}});if(startNode)console.warn('PlebVox: START marker has no matching END marker');console.log('PlebVox 3.1: found '+sections.length+' readable section(s)');return sections;}
    function isWordChar(ch){if(!ch)return false;try{return/[\p{L}\p{N}\p{M}_]/u.test(ch);}catch(e){return/[A-Za-z0-9_]/.test(ch);}}
    function wordBounds(text,index){if(!text)return null;let i=Math.max(0,Math.min(index,text.length-1));if(!isWordChar(text[i])&&i>0&&isWordChar(text[i-1]))i--;if(!isWordChar(text[i]))return null;let a=i,b=i+1;while(a>0&&isWordChar(text[a-1]))a--;while(b<text.length&&isWordChar(text[b]))b++;return{start:a,end:b};}
    function normalizedToRaw(rawText,offset){let raw=0,norm=0,ws=false;while(raw<rawText.length&&norm<offset){const ch=rawText[raw];if(/\s/.test(ch)){if(!ws){norm++;ws=true;}}else{ws=false;norm++;}raw++;}return raw;}
    function findMappingEntry(section,charIndex){const m=section&&section.mappingData&&section.mappingData.mapping||[];for(let i=0;i<m.length;i++){if(charIndex>=m[i].speechStart&&charIndex<m[i].speechEnd)return m[i];}for(let i=0;i<m.length;i++){if(charIndex===m[i].speechEnd)return m[i];}return null;}
    function highlightWordByCharIndex(charIndex,section){clearHighlights();if(!section||!section.mappingData)return;const entry=findMappingEntry(section,charIndex);if(!entry||!entry.node||!entry.node.parentNode)return;let relative=charIndex-entry.speechStart;if(relative>=entry.normalizedText.length)relative=entry.normalizedText.length-1;const bounds=wordBounds(entry.normalizedText,relative);if(!bounds)return;const rawStart=normalizedToRaw(entry.rawText,bounds.start),rawEnd=normalizedToRaw(entry.rawText,bounds.end);if(rawEnd<=rawStart)return;if(customHighlightSupported){try{const range=new Range();range.setStart(entry.node,rawStart);range.setEnd(entry.node,rawEnd);CSS.highlights.set(HIGHLIGHT_NAME,new Highlight(range));return;}catch(e){console.warn('PlebVox 3.1: CSS highlight failed; using fallback',e);}}const node=entry.node,p=node.parentNode,raw=node.textContent||'';const before=document.createTextNode(raw.slice(0,rawStart)),word=document.createTextNode(raw.slice(rawStart,rawEnd)),after=document.createTextNode(raw.slice(rawEnd));const span=document.createElement('span');span.className='plebvox-highlight';span.appendChild(word);if(before.nodeValue)p.insertBefore(before,node);p.insertBefore(span,node);if(after.nodeValue)p.insertBefore(after,node);p.removeChild(node);rebuildSectionMapping(section);}
    function speakSection(text,section,controls){if(!window.speechSynthesis){alert('Speech synthesis is not available in this browser.');return;}if(isReading&&currentSectionData===section){if(isPaused)resumeSpeech(controls);else pauseSpeech(controls);return;}if(isReading)stopSpeech(activeControls);rebuildSectionMapping(section);const speechText=section.mappingData.speechText;if(!speechText||speechText.length<2){alert('No readable content in this section.');return;}currentSectionData=section;activeControls=controls;utterance=new SpeechSynthesisUtterance(speechText);utterance.rate=speechRate;utterance.pitch=1;utterance.volume=1;waitForVoices().then(function(voices){availableVoices=voices;if(voices.length){voicesLoaded=true;voiceLoadComplete=true;updateAllVoiceSelectors();updateVoiceLoadStatus();if(!selectedVoice||!voices.includes(selectedVoice))selectedVoice=voices.find(v=>/^en[-_]ZA$/i.test(v.lang))||voices.find(v=>/^en[-_]GB$/i.test(v.lang))||voices.find(v=>/^en[-_]AU$/i.test(v.lang))||voices.find(v=>/^en/i.test(v.lang))||voices[0];if(selectedVoice)utterance.voice=selectedVoice;}utterance.onstart=function(){isReading=true;isPaused=false;clearHighlights();updateControls(controls,'playing');};utterance.onboundary=function(event){if(typeof event.charIndex!=='number'||event.charIndex<0)return;console.debug('PlebVox 3.1 boundary:',event.name||'(unnamed)',event.charIndex,event.charLength||0);highlightWordByCharIndex(event.charIndex,section);};utterance.onend=function(){isReading=false;isPaused=false;clearHighlights();updateControls(controls,'idle');activeControls=null;currentSectionData=null;};utterance.onerror=function(event){console.error('PlebVox speech error:',event);isReading=false;isPaused=false;clearHighlights();updateControls(controls,'idle');activeControls=null;currentSectionData=null;};try{window.speechSynthesis.cancel();window.speechSynthesis.speak(utterance);}catch(e){console.error('PlebVox: failed to speak',e);updateControls(controls,'idle');}});}
    function pauseSpeech(controls){if(isReading&&!isPaused){window.speechSynthesis.pause();isPaused=true;updateControls(controls,'paused');}}
    function resumeSpeech(controls){if(isReading&&isPaused){window.speechSynthesis.resume();isPaused=false;updateControls(controls,'playing');}}
    function stopSpeech(controls){try{window.speechSynthesis.cancel();}catch(e){}isReading=false;isPaused=false;clearHighlights();if(controls)updateControls(controls,'idle');activeControls=null;currentSectionData=null;}
    function updateControls(c,state){if(!c)return;c.playBtn.style.display='none';c.pauseBtn.style.display='none';c.resumeBtn.style.display='none';c.stopBtn.style.display='none';if(state==='playing'){c.pauseBtn.style.display='inline-block';c.stopBtn.style.display='inline-block';c.statusText.textContent='Playing...';}else if(state==='paused'){c.resumeBtn.style.display='inline-block';c.stopBtn.style.display='inline-block';c.statusText.textContent='Paused';}else{c.playBtn.style.display='inline-block';c.statusText.textContent='Ready';}}
    function generateUniqueId(prefix){sectionCounter++;return prefix+'-'+sectionCounter+'-'+Date.now().toString(36);}
    function waitForVoices(){if(voicePromise)return voicePromise;voicePromise=new Promise(function(resolve){if(!('speechSynthesis'in window)){resolve([]);return;}let voices=window.speechSynthesis.getVoices();if(voices.length){resolve(voices);return;}function changed(){voices=window.speechSynthesis.getVoices();if(voices.length){window.speechSynthesis.removeEventListener('voiceschanged',changed);resolve(voices);}}window.speechSynthesis.addEventListener('voiceschanged',changed);try{window.speechSynthesis.cancel();}catch(e){}setTimeout(function(){voices=window.speechSynthesis.getVoices();if(voices.length){window.speechSynthesis.removeEventListener('voiceschanged',changed);resolve(voices);return;}setTimeout(function(){window.speechSynthesis.removeEventListener('voiceschanged',changed);resolve(window.speechSynthesis.getVoices());},1500);},500);});return voicePromise;}
    function loadVoices(){if(voicesLoaded)return;waitForVoices().then(function(v){availableVoices=v;voiceLoadComplete=true;if(v.length){selectedVoice=v.find(x=>/^en[-_]ZA$/i.test(x.lang))||v.find(x=>/^en[-_]GB$/i.test(x.lang))||v.find(x=>/^en/i.test(x.lang))||v[0];voicesLoaded=true;}updateAllVoiceSelectors();updateVoiceLoadStatus();}).catch(function(e){console.warn('PlebVox: voice loading error',e);voiceLoadComplete=true;updateVoiceLoadStatus();});}
    function getBrowserInfo(){const ua=navigator.userAgent.toLowerCase();if(navigator.brave)return'Brave';if(ua.includes('vivaldi'))return'Vivaldi';if(ua.includes('edg'))return'Edge';if(ua.includes('firefox'))return'Firefox';if(ua.includes('chrome'))return'Chrome';if(ua.includes('safari'))return'Safari';return'Unknown';}
    function updateVoiceLoadStatus(){document.querySelectorAll('.plebvox-voice-status').forEach(function(el){if(!('speechSynthesis'in window)){el.textContent='⚠️ Speech synthesis not supported';el.style.color='#dc3545';}else if(!voiceLoadComplete){el.textContent='⏳ Loading voices...';el.style.color='#17a2b8';}else if(availableVoices.length){el.textContent='✅ '+availableVoices.length+' voice(s) available';el.style.color='#28a745';}else{el.textContent='ℹ️ No system voices ('+getBrowserInfo()+')';el.style.color='#b38600';}});}
    function updateAllVoiceSelectors(){document.querySelectorAll('.plebvox-voice-select').forEach(populateVoiceSelector);}
    function populateVoiceSelector(select){const current=select.value;select.innerHTML='';if(!availableVoices.length){const o=document.createElement('option');o.value='';o.textContent='No voices available';select.appendChild(o);return;}availableVoices.forEach(function(v){const o=document.createElement('option');o.value=v.name;let f='🌐';if(/^en[-_]GB$/i.test(v.lang))f='🇬🇧';else if(/^en[-_]US$/i.test(v.lang))f='🇺🇸';else if(/^en[-_]AU$/i.test(v.lang))f='🇦🇺';else if(/^en[-_]ZA$/i.test(v.lang))f='🇿🇦';o.textContent=f+' '+v.name;select.appendChild(o);});if(current&&availableVoices.some(v=>v.name===current))select.value=current;else if(selectedVoice)select.value=selectedVoice.name;}
    function createSectionControls(section,index){const container=document.createElement('div');container.className='plebvox-control-'+index;container.style.cssText='margin:.75rem 0;padding:.75rem 1rem;max-width:100%;background:var(--secondary-nav-bg,#f4f4f4);border-radius:8px;border:1px solid var(--border,#ddd);text-align:center;';const title=document.createElement('div');title.textContent='🔊 PlebVox — Part '+section.number;title.style.cssText='font-weight:700;font-size:1rem;color:var(--text,#000);margin-bottom:.5rem;text-align:center;';const bc=document.createElement('div');bc.style.cssText='display:flex;justify-content:center;align-items:center;gap:.5rem;flex-wrap:wrap;margin-bottom:.5rem;';function btn(t,l,css,d){const b=document.createElement('button');b.textContent=t;b.setAttribute('aria-label',l);b.style.cssText=css+'display:'+d+';';return b;}const base='padding:.4rem 1rem;border:none;border-radius:4px;cursor:pointer;font-size:.9rem;font-weight:600;';const play=btn('▶ Play','Play Part '+section.number,base+'background:#28a745;color:#fff;','inline-block');const pause=btn('⏸ Pause','Pause Part '+section.number,base+'background:#ffc107;color:#000;','none');const resume=btn('▶ Resume','Resume Part '+section.number,base+'background:#17a2b8;color:#fff;','none');const stop=btn('⏹ Stop','Stop Part '+section.number,base+'background:#dc3545;color:#fff;','none');const status=document.createElement('span');status.textContent='Ready';status.setAttribute('aria-live','polite');status.style.cssText='font-size:.8rem;color:var(--text-secondary,#666);margin-left:.5rem;min-width:60px;';const controls={playBtn:play,pauseBtn:pause,resumeBtn:resume,stopBtn:stop,statusText:status};play.addEventListener('click',()=>speakSection(section.text,section,controls));pause.addEventListener('click',()=>pauseSpeech(controls));resume.addEventListener('click',()=>resumeSpeech(controls));stop.addEventListener('click',()=>stopSpeech(controls));bc.append(play,pause,resume,stop,status);const row=document.createElement('div');row.style.cssText='display:flex;justify-content:center;align-items:center;gap:.75rem;flex-wrap:wrap;margin-top:.25rem;';const sid=generateUniqueId('plebvox-speed'),sl=document.createElement('label');sl.textContent='Speed:';sl.setAttribute('for',sid);sl.style.cssText='font-size:.8rem;color:var(--text-secondary,#666);';const speed=document.createElement('input');speed.type='range';speed.id=sid;speed.min='.3';speed.max='1.8';speed.step='.05';speed.value=speechRate;speed.setAttribute('aria-label','Reading speed for Part '+section.number);speed.setAttribute('aria-valuenow',speechRate);speed.setAttribute('aria-valuemin','0.3');speed.setAttribute('aria-valuemax','1.8');speed.style.cssText='width:100px;cursor:pointer;';const sd=document.createElement('span');sd.textContent='0.70x';sd.setAttribute('aria-live','polite');sd.style.cssText='font-size:.8rem;min-width:3rem;color:var(--text-secondary,#666);';speed.addEventListener('input',function(){speechRate=parseFloat(this.value);sd.textContent=speechRate.toFixed(2)+'x';this.setAttribute('aria-valuenow',speechRate);});const vid=generateUniqueId('plebvox-voice'),vl=document.createElement('label');vl.textContent='Voice:';vl.setAttribute('for',vid);vl.style.cssText='font-size:.8rem;color:var(--text-secondary,#666);';const voice=document.createElement('select');voice.id=vid;voice.className='plebvox-voice-select';voice.setAttribute('aria-label','Select voice for Part '+section.number);voice.style.cssText='padding:.2rem .4rem;border-radius:4px;border:1px solid var(--border,#ddd);font-size:.75rem;max-width:180px;';populateVoiceSelector(voice);voice.addEventListener('change',function(){const v=availableVoices.find(x=>x.name===this.value);if(v)selectedVoice=v;});const vs=document.createElement('span');vs.className='plebvox-voice-status';vs.setAttribute('aria-live','polite');vs.style.cssText='font-size:.7rem;color:var(--text-secondary,#666);margin-left:.25rem;';row.append(sl,speed,sd,vl,voice,vs);container.append(title,bc,row);return container;}
    function initPlebVox(){installHighlightStyle();if(!('speechSynthesis'in window)){console.warn('PlebVox: speech synthesis unavailable');return;}sectionDataList=getPlebVoxSections();if(!sectionDataList.length)return;loadVoices();const main=document.querySelector('main');if(!main)return;sectionDataList.forEach(function(section,index){const control=createSectionControls(section,index);section.controlElement=control;section.startNode.parentNode.insertBefore(control,section.startNode.nextSibling);});window.speechSynthesis.addEventListener('voiceschanged',function(){const v=window.speechSynthesis.getVoices();if(v.length){availableVoices=v;voicesLoaded=true;voiceLoadComplete=true;updateAllVoiceSelectors();updateVoiceLoadStatus();}});setTimeout(updateVoiceLoadStatus,1000);}
    if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initPlebVox);else initPlebVox();
})();
